---
CLIENT_CERTIFICATES:
  - user1
  - user2

O: OpenCore
C: DE
ST: SH
L: Wedel
OU: Internal

DOMAIN: OPENCORE.COM

CERTIFICATE_VALID_DAYS: 365
KEY_VALID_DAYS: 10000

SSL_STORE_PASSWORD: secret
SSL_KEY_PASSWORD: secret

FORCE_CA: false
FORCE_CERTS: false

KEYSTORE_DIR: "{{ KEYSTORE_BASE_DIR }}"
CA_DIR: "{{ KEYSTORE_BASE_DIR }}/ca"
CERT_DIR: "{{ KEYSTORE_BASE_DIR }}/cert"